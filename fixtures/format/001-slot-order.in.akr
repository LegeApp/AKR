akr 0.1
project fixtures

record fx.decision.ordering/1 : decision {
    supersedes [ @fx.decision.ordering-old/1 ]
    created_at 2026-08-03
    decision """
        Slot order is enforced, not suggested.
        """
    state active
    implements [ @fx.req.something ]
    title "Canonical slot order"
    author "dkoepke"
    topic ordering
    scope [ all ]
    context """
        The input here is in an order a person might plausibly type.
        """
}
