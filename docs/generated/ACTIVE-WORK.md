<!-- GENERATED BY AKR — DO NOT EDIT
     source-graph: sha256:20f0e3da815a4da0c629778d9c51af2ff68b4442dc8135e7e53b2d7885ee5a45
     tool: akr 0.3.0
-->

# Active work — akr

Live work, grouped by parent in `ROADMAP.md` order. Blocked work names its blocker inline.

## [Deliver native human-facing AKR record review](ROADMAP.md#deliver-native-human-facing-akr-record-review) `@akr.milestone.human-record-review/1`

### Certify Windows and Linux GUI builds

`proposed` · `@akr.work.windows-linux-certification/1` · part of `@akr.milestone.human-record-review/1`

Provide CI build and deterministic software-presenter test coverage for the native AKR GUI on Windows and Linux.

**Acceptance** — 0 of 1 satisfied

| Check | Method | Verdict |
| --- | --- | --- |
| `platform-ci` | observation | not satisfied — no evidence |

## Unparented

### Make AKR authoring contracts discoverable before writes

`proposed` · `@akr.work.authoring-contracts/1`

Make CLI help, MCP schemas, and write diagnostics expose the actual per-kind slot and
relation contract, typed commit fields, namespace requirements, accepted `--from`
shape, lock-refresh next step, and scope-glob mistakes before an agent has to learn
them through repeated rejected or silently ineffective writes.

**Acceptance** — 0 of 3 satisfied

| Check | Method | Verdict |
| --- | --- | --- |
| `scope-sanity` | command | not satisfied — no evidence |
| `typed-kind-contract` | command | not satisfied — no evidence |
| `workflow-remedies` | command | not satisfied — no evidence |

### Make MCP sessions survive sustained mixed AKR use

`proposed` · `@akr.work.mcp-transport-reliability/1`

Reproduce and eliminate the recurrent sister-project `Transport closed` failures seen
after successful reads and writes. Cover the installed connector process, not only an
in-memory protocol loop, and preserve a useful structured failure if the server must
terminate.

**Acceptance** — 0 of 3 satisfied

| Check | Method | Verdict |
| --- | --- | --- |
| `installed-session-stress` | command | not satisfied — no evidence |
| `sister-reproduction` | manual | not satisfied — no evidence |
| `termination-diagnostic` | command | not satisfied — no evidence |

### Align context and supersession reference contracts

`proposed` · `@akr.work.reference-supersession-consistency/1`

Remove surface contradictions around canonical references and replacement: context
should either normalize or precisely reject pinned and terminal planning references,
and advertised different-key supersession must be implemented atomically or removed
from the public contract with a supported two-step replacement workflow.

**Acceptance** — 0 of 3 satisfied

| Check | Method | Verdict |
| --- | --- | --- |
| `context-reference-contract` | command | not satisfied — no evidence |
| `different-key-contract` | command | not satisfied — no evidence |
| `recovery-guidance` | command | not satisfied — no evidence |

### Preserve requested lifecycle state across revisions

`proposed` · `@akr.work.revise-lifecycle-semantics/1`

Make sealed-record revision behavior explicit and correct: a requested state must land
on the new revision, while a content-only revision must follow one documented state
policy consistently across CLI and MCP instead of accidentally returning accepted
records to `proposed`.

**Acceptance** — 0 of 3 satisfied

| Check | Method | Verdict |
| --- | --- | --- |
| `content-state-policy` | command | not satisfied — no evidence |
| `ready-to-active` | command | not satisfied — no evidence |
| `surfaces-agree` | command | not satisfied — no evidence |
