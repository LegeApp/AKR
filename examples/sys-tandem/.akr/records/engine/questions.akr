akr 0.1
project sys-tandem

record engine.question.voice-direction/1 : question {
    title "What should the court sound like, and who records it?"
    state deferred
    question """
        Real voice direction — what the court should sound like, casting, recording —
        is a separate designer track with no scheduled milestone. Placeholder TTS
        carries every testing objective in the meantime, so nothing is blocked. When
        does this become worth scheduling, and against what budget?
        """
    created_at 2026-08-03
}
