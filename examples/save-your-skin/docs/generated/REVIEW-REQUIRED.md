<!-- GENERATED BY AKR — DO NOT EDIT
     source-graph: sha256:5159d2714f688627a85906b161def17b0358155fbfc2a3fc00b681d29a305f55
     commit: e806b3f54a2d7091c5e13b8a26f490dc7b135e64
     tool: akr 0.1.0
-->

# Review required — save-your-skin

Records that should not be relied on without a second look, derived at commit
`e806b3f5` with today taken as 2026-08-03.

**Nothing here is a diagnostic and nothing here is an error.** Staleness is a build fact
(D-024): `akr check` exits 0 with this list non-empty, and no record below has had its
state, its content, or the truth of any claim changed. A stale record is a question
raised, not an answer given.

**An empty file on an active project means the `watches` globs are wrong**, not that the
knowledge is perfect. Two stale records across forty on a project mid-milestone is a
healthy number.

**2 stale · 4 at risk · maximum propagation depth 2**

## Stale (2)

Records whose own basis has moved. Ordered by cause — a watched path that moved before a
review date that passed — then by the matching commit, most recent first.

### Projection coverage is thinnest at day boundaries

`verified` · `@sim.obs.projection-gaps/1` · observation ·
[CURRENT-STATE.md](CURRENT-STATE.md#projection-coverage-is-thinnest-at-day-boundaries)

**Cause** — `watches "sim/src/project/**"` was matched by `5d9c2a70`, which is not
reachable from `observed_at 7c41d0ba`.

**Matched path** — `sim/src/project/` (C4, 2 commits after the observation)

The projection code this observation measured has changed since it was measured. The
coverage numbers in it describe code that no longer exists in that form.

**Suggested action** — re-run the coverage measurement and
`akr revise sim.obs.projection-gaps` with the new `observed_at`, or supersede it. Two
records downstream clear when it does.

### Long runs drift by one tick over an in-game week

`verified` · `@sim.obs.timestep-drift/1` · observation ·
[CURRENT-STATE.md](CURRENT-STATE.md#long-runs-drift-by-one-tick-over-an-in-game-week)

**Cause** — `review_after 2026-07-15` has passed (19 days ago).

The watched path `sim/src/step.rs` has *not* moved since `observed_at 5d9c2a70`; this
record is queued by its review date alone. It was given a short review window because it
contradicts `@sim.evidence.determinism-suite-pass/1`, and an acknowledged contradiction
is exactly the kind of thing that should be looked at again on a timer.

**Suggested action** — re-run the week-long divergence test. If the drift is gone, this
observation is disproven and the contradiction resolves itself.

## At risk (4)

Records that rest on something stale, along `supported_by`, `depends_on` or
`derived_from` only (D-024). Ordered by propagation depth, then by key. None of these has
been changed; each is flagged because its support has aged.

### Rewrite the projection pass

`blocked` · `@sim.work.rewrite-projection/1` · work · **depth 1** ·
[ACTIVE-WORK.md](ACTIVE-WORK.md#rewrite-the-projection-pass)

**Via** `depends_on` → `@sim.obs.projection-gaps/1` (stale: watched path moved)

The premise of this work item is the coverage measurement that motivated it, and that
measurement is stale. Anyone picking the item up should re-measure before deciding what
the rewrite has to achieve. Propagation is not restricted to records that make claims
about the world: any kind that declares its correctness rests on something stale is
flagged.

### M3 is one blocked work item away from ready

`verified` · `@sys.assessment.m3-readiness/1` · assessment · **depth 1** ·
[CURRENT-STATE.md](CURRENT-STATE.md#m3-is-one-blocked-work-item-away-from-ready)

**Via** `supported_by` → `@sim.obs.timestep-drift/1` (stale: `review_after` passed)

### Projection gaps put the M3 date at risk

`verified` · `@sys.assessment.projection-gaps/1` · assessment · **depth 1** ·
[CURRENT-STATE.md](CURRENT-STATE.md#projection-gaps-put-the-m3-date-at-risk)

**Via** `supported_by` → `@sim.obs.projection-gaps/1` (stale: watched path moved)

### Engine and simulator advance in tandem

`active` · `@sys.policy.tandem-work/1` · policy · **depth 2** ·
[CURRENT-STATE.md](CURRENT-STATE.md#engine-and-simulator-advance-in-tandem)

**Via** `supported_by` → `@sys.assessment.projection-gaps/1` → `@sim.obs.projection-gaps/1`
(stale: watched path moved)

This is the entry that justifies propagation. A live governance policy that nobody has
touched, whose supporting assessment rests on an observation made before the projection
code changed. No reader of the policy would have known. The graph knew.

## Not flagged

For contrast, and because "why is this *not* here?" is asked as often as the reverse:

| Record | Why not flagged |
| --- | --- |
| `@lege.obs.frame-budget-headroom/1` | `observed_at` is C5, which is HEAD. C5 touched `lege/src/render/**`, which it watches, but the observation was made *at* that commit. |
| `@lege.obs.viewer-imports-engine/1` | `disproven` — terminal records are not evaluated for freshness at all. |
| `@sys.policy.no-hand-edited-views/1` | Nothing it rests on is stale; it has no `supported_by` edge. |
| `@sys.milestone.m3-playable-day/1` | Staleness does not propagate along `part_of`, `after`, `plan_of_record`, `implements` or `blocks`. A milestone is not endangered by one aged observation beneath it — but `@sim.work.rewrite-projection/1` beneath it is, because that record declares `depends_on`. |
| `@sim.question.timestep-vs-budget/1` | It `blocks` an at-risk work item, and `blocks` does not propagate. A blocker is not endangered by the thing it blocks. |
